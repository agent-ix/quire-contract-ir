{
  "schema_version": "quire-contract-ir.assurance-pins/v1",
  "purpose": "The Engineering Assurance release this repository adopts, and the digests of the artifacts it actually reads from that release. Component version pins are NOT restated here: the packaged compatibility matrix is their authority and a second copy would drift from it.",
  "engineering_assurance": {
    "version": "0.2.0",
    "release": "git tag v0.2.0 of agent-ix/engineering-assurance",
    "release_form": "source distribution; npm publication is refused by design, so the tag is the released artifact",
    "requirement": "requirements-assurance.txt",
    "consumed_artifacts": [
      {
        "path": "compatibility.py",
        "sha256": "62829251d7697d279364eeb2395a1e86a81ef116703c4b89ec754f701475f654",
        "why": "load_matrix, classify_all and accepted are the authority scripts/check_shared_pins.py delegates every version verdict to. This digest is the file as the v0.2.0 tag carries it; the module on engineering-assurance main differs, so a pin that matched main would not be a pin on the release."
      },
      {
        "path": "compatibility-matrix.json",
        "why": "The authority on which component versions are pinned. Deliberately carries no digest here: it is expected to advance as the matrix is re-accepted, and pinning its bytes locally would make this repository a second acceptance authority."
      }
    ]
  },
  "matrix_authority": "engineering_assurance/compatibility-matrix.json in the installed distribution. scripts/check_shared_pins.py observes the local toolchain and delegates every verdict to engineering_assurance.compatibility.",
  "registry": "Every npm component resolves from the public npm registry (registry.npmjs.org). The internal npm.ix mirror must not appear in any requirement, pin, lockfile, or .npmrc, and is not an oracle for whether a release landed.",
  "known_drift": [
    "The v0.2.0 tag records acceptance state 'pending_human_acceptance' and its compatibility module has no human_acceptance_recorded predicate at all: both landed on engineering-assurance main after the tag (48daacd, ae50e13) and no v0.2.1 was cut. The acceptance gate is therefore satisfiable only from that branch head, which is the one thing FR-012-AC-1 says a pin may not be. The named human's acceptance is genuine and attributed in that repository; what is missing is a release carrying it. Filed as agent-ix/engineering-assurance#20.",
    "scripts/check_shared_pins.py reports the acceptance state it finds and gates only on the local toolchain, so neither an absent field nor a branch head is read as an approval."
  ],
  "retired_pins": [
    "verification_semantics.py (af9f92fc...) and schemas/pgm01-compatibility-view-v1.schema.json (317294d0...) were pinned because scripts/pgm01_compatibility_view.py read this repository's retained PGM-01 history through map_pgm01_bytes. The owner released the pre-stable evidence-preservation constraint on 2026-09-02 (agent-ix/engineering-assurance#7), the retained records and their reader are deleted, and nothing here consumes either artifact any more. They were removed rather than left in place: a pin on a file no gate reads makes 'the artifacts it actually reads' false, and a digest check that never runs against a live consumer is a green light nobody earned. compatibility.py replaces them so the digest-drift check still binds to something this repository genuinely imports."
  ]
}

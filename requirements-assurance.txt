# The shared assurance lane (FR-022). Install this in its OWN environment.
#
# `make assurance-env` builds .venv-assurance from this file. It stays a separate
# environment so the pinned distribution and its transitive jsonschema>=4.23,<5
# requirement cannot collide with anything installed system-wide. The Draft 7
# governance lane that used to be the other half of this split is deleted with
# PGM-01-R08.
#
# The pin is the accepted release from agent-ix/engineering-assurance#8, which
# publishes engineering-assurance as a source distribution: the git tag IS the
# released artifact. It is not a branch head and must not be replaced by one.
engineering-assurance @ git+https://github.com/agent-ix/engineering-assurance@v0.2.0
